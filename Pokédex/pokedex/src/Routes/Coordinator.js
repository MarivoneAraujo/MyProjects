export const goToHome = (history) => {
    history.push("/");
  };

  export const goToPokeDetails = (history, name) => {
    history.push(`/pokedetails/${name}`);
  };
  
  export const goToPokeList = (history) => {
    history.push("/pokelist");
  };





  